<div class="container">
	
	<div class="page-header">
		<h1>Blocipediers<small> ...unite!</small></h1>
	</div>

	<%= @users.each do |user| %>

		<div class="row">
			<div class="col-md-8">

				<h4><%= link_to user.username, user %></h4>
			
			</div>
		</div>

<% end %>

<hr class="divider">
		<div class="paginate">
			<%= will_paginate @users, renderer: BootstrapPagination::Rails %><br>
		</div>

</div>
